[package]
name = "rim_client"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/oddurs/rim"

[[bin]]
name = "rim"
path = "src/main.rs"

[dependencies]
rim_sim = { path = "../rim_sim" }
rim_ui = { path = "../rim_ui" }
macroquad = "0.4"
# The player's settings file; already built for the sim and the UI.
toml = "1"
