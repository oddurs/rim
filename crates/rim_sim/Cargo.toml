[package]
name = "rim_sim"
version = "0.1.0"
edition = "2021"

[dependencies]
hecs = "0.10"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
mlua = { version = "0.10", features = ["luau"] }
