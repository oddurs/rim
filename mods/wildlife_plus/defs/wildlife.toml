# Tier 1 — data. A new creature; map generation picks it up automatically.
[[creature]]
id = "boar"
label = "boar"
color = "#5a4636"
size = 0.38
speed = 12
max_hp = 80
melee_damage = 8
melee_cooldown = 80
market_value = 70
butcher = [{ thing = "raw_meat", count = 30 }]
spawn = { terrain = ["dirt", "grass"], groups = 2, group = [1, 3] }

# Tier 1 — patches. Change core content without copying it.
[[patch]]
target = "thing/berry_bush"
set = { harvest = { regrow_days = 1.5 } }
