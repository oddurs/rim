# Tier 1 — data. A new creature; map generation picks it up automatically.
[[creature]]
id = "boar"
label = "boar"
color = "#5a4636"
size = 0.38
speed = 12
max_hp = 80
melee_damage = 8
melee_cooldown = 80
market_value = 70
butcher = [{ thing = "core:raw_meat", count = 30 }]
spawn = { terrain = ["core:dirt", "core:grass"], groups = 2, group = [1, 3] }

# Tier 1 — patches. Change core content without copying it.
[[patch]]
target = "thing/core:berry_bush"
[[patch.edit]]
list = "harvest"
match = { designation = "harvest" }
set = { regrow_days = 1.5 }

# Tier 1 — art. A mod's sprite: sprites/salt_lick.png, packed into the one
# world atlas with every other mod's, drawn as painted over a shadow.
[[thing]]
id = "salt_lick"
label = "salt lick"
color = "#e8e2d6"
category = "building"
path_cost = 60
market_value = 12
build = { menu = "furniture", work = 120, stuff = { category = "structural", count = 6 } }
look.layers = [
    { draw = "disc", x = 0.54, y = 0.84, r = 0.3, color = "#00000030" },
    { draw = "sprite", sprite = "salt_lick", x = 0.1, y = 0.05, w = 0.8, h = 0.8 },
]

# Tier 1 — a footprint. Two cells wide and one entity: the look is laid
# out across both and drawn once, from its anchor (DESIGN.md §6a).
[[thing]]
id = "feeding_trough"
label = "feeding trough"
color = "#8a6a45"
category = "building"
size = [2, 1]
path_cost = 60
market_value = 10
build = { menu = "furniture", work = 150, stuff = { category = "structural", count = 8 } }
look.layers = [
    { draw = "fill", x = 0.05, y = 0.32, w = 0.93, h = 0.5, color = "#00000030" },
    { draw = "fill", x = 0.02, y = 0.25, w = 0.96, h = 0.5 },
    { draw = "fill", x = 0.06, y = 0.34, w = 0.88, h = 0.28, color = "#c9b36a" },
    { draw = "outline", x = 0.02, y = 0.25, w = 0.96, h = 0.5, width = 1.0, shade = 0.6 },
]

# Tier 1 — a glyph. A character as a look, rasterised once at load into the
# world atlas: legible at any zoom, and no art to ship.
[[thing]]
id = "trail_marker"
label = "trail marker"
color = "#d8b04a"
category = "building"
path_cost = 20
market_value = 2
build = { menu = "furniture", work = 40, stuff = { category = "structural", count = 1 } }
look.layers = [
    { draw = "disc", r = 0.3, color = "#00000040" },
    { draw = "glyph", glyph = "▲", size = 0.6, color = "#e6c04e" },
]
